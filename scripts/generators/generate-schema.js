module.exports = async function generateSchema(context) {
  console.log("[schema] Scaffold only. Future implementation will emit JSON-LD and schema payloads.");
  console.log(`[schema] root=${context.root} mode=${context.mode}`);
};
