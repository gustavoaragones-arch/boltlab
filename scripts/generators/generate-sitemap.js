module.exports = async function generateSitemap(context) {
  console.log("[sitemap] Scaffold only. Future implementation will use generated routes.");
  console.log(`[sitemap] root=${context.root} mode=${context.mode}`);
};
