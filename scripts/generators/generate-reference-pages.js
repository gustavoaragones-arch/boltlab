module.exports = async function generateReferencePages(context) {
  console.log("[reference] Scaffold only. Future implementation will consume knowledge objects.");
  console.log(`[reference] root=${context.root} mode=${context.mode}`);
};
