module.exports = async function generateApiData(context) {
  console.log("[api] Scaffold only. Future implementation will output normalized API payloads.");
  console.log(`[api] root=${context.root} mode=${context.mode}`);
};
