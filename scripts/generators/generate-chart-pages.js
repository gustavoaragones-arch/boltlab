module.exports = async function generateChartPages(context) {
  console.log("[charts] Scaffold only. Future implementation will consume canonical datasets.");
  console.log(`[charts] root=${context.root} mode=${context.mode}`);
};
